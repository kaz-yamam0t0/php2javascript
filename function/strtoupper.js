/** 
 * Javascript equivalent to php `strtoupper`
 * 
 * Make a string uppercase
 * @see https://www.php.net/manual/en/function.strtoupper.php
 * 
 * @param string s
 * @return string
 */
const strtoupper = (s)=>s.toUpperCase()

module.exports = {strtoupper}